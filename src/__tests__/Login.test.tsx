 //import React from 'react';
 //import { render, fireEvent, waitForElement, findByTestId } from '@testing-library/react';
 import { isFoo } from '../App'
 //import { Post } from '../components/Post';
 //import { Login } from '../components/Login';

 test('should return true given foo', () => {
    expect(isFoo('foo')).toBe(true)
  })

//  describe("<Login />", () => {
//      test("This should display an empty form with placeholders for logging into Sticky", async () => {

//      });
//  });

